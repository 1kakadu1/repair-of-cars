export enum RoutsPath{
    products = "/products",
    product = "/product",
    services = "/services",
    home = "/",
    not_found = '/404'
}

export enum DefaultPrice{
    min = 500,
    max = 50000
}